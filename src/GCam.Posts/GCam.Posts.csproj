<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Consumes machine-neutral CLData from Core and emits G-code.
    XML-driven templates for now; a script engine slots in behind the same
    interface later. netstandard2.0 for the same reasons as Core.
  -->
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <RootNamespace>GCam.Posts</RootNamespace>
    <AssemblyName>GCam.Posts</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\GCam.Core\GCam.Core.csproj" />
  </ItemGroup>

</Project>
